<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    tools:background="#000000"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/gridList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginStart="4dp"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="8dp"
        android:foregroundGravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txtAccount"
        app:layout_constraintVertical_bias="1.0" />

    <TextView
        android:id="@+id/txtAccount"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="16dp"
        android:text="@ruslan.lepekha"
        android:textColor="?attr/color_4"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/btnAccSwitch"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/btnAccAdd"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?attr/color_4"
        app:layout_constraintBottom_toBottomOf="@+id/txtAccount"
        app:layout_constraintEnd_toStartOf="@+id/btnAccRemove"
        app:layout_constraintTop_toTopOf="@+id/txtAccount"
        app:srcCompat="@drawable/ic_instagram_planer_person_add" />

    <ImageButton
        android:id="@+id/btnAccRemove"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="16dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?attr/color_4"
        app:layout_constraintBottom_toBottomOf="@+id/btnAccAdd"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/btnAccAdd"
        app:srcCompat="@drawable/ic_instagram_planer_person_remove" />

    <ImageButton
        android:id="@+id/btnAccSwitch"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="?attr/color_4"
        app:layout_constraintBottom_toBottomOf="@+id/btnAccAdd"
        app:layout_constraintEnd_toStartOf="@+id/btnAccAdd"
        app:layout_constraintTop_toTopOf="@+id/btnAccAdd"
        app:srcCompat="@drawable/ic_instagram_planer_switch_account" />

</androidx.constraintlayout.widget.ConstraintLayout>